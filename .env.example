# ===========================================
# PPM - Petrol Pump Management System
# Environment Configuration Template
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!
# ===========================================

# Database Configuration
# For Docker PostgreSQL: use 'postgres' container name as host
# For External PostgreSQL: use 'localhost' or 'host.docker.internal' (from Docker)
POSTGRES_DB=PPM
POSTGRES_USER=ppm_user
POSTGRES_PASSWORD=your_secure_password_here

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long
JWT_ISSUER=PPM.API
JWT_AUDIENCE=PPM.Client
JWT_EXPIRATION=30

# API Configuration
# For local development:
# NEXT_PUBLIC_API_URL=http://localhost:5002/api
# For Contabo VPS (75.119.129.104):
NEXT_PUBLIC_API_URL=http://75.119.129.104:5002/api

# Optional: SQL Server (if using SQL Server instead of PostgreSQL)
# SA_PASSWORD=YourStrong@Password123

# Optional: Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourcompany.com

# ===========================================
# Contabo VPS Deployment
# Server IP: 75.119.129.104
# ===========================================
# DOMAIN=yourdomain.com
# SSL_EMAIL=admin@yourdomain.com
